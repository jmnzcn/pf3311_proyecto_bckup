# Área de trabajo local (no versionada)

Carpeta personal en `.gitignore`. **No forma parte del repositorio público** ni de lo que describe el pipeline en `_tools/README.md`.

## Qué va en el repo público

- Código Unity + `_tools/analyze_all_rq.py` → tablas y figuras en `_analysis/`.

## Qué se entrega aparte (fuera del pipeline)

- **Documentos del curso** (`docs/`: Entregables, protocolo, meCUE…).
- **Informe de resultados del estudio** (redacción propia usando `_analysis/` como insumo).

## Contenido opcional de esta carpeta

Borradores, ensayos de Word/PDF o scripts auxiliares (`scripts/build.py`, etc.) para uso local. No implica que el informe se genere automáticamente en el entregable de código.
